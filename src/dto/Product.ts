class Product {
    private product_id: number;
    private value: number;

    constructor(product_id: number, value: number) {
        this.product_id = product_id;
        this.value = value;
    }
}

export default Product;